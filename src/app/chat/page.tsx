import StormDrainLayout from "@/components/StormDrainLayout";

/**
 * /chat 경로: Chat 화면.
 * F5 새로고침 시에도 이 URL이 유지되면 Chat 화면이 그대로 표시됩니다.
 */
export default function ChatPage() {
  return <StormDrainLayout />;
}
